apiVersion: v1
kind: Service
metadata:
  name: pireborn
  labels:
    app: pireborn
spec:
  type: {{ .Values.service.type }}
  selector:
    app: pireborn
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP